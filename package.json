{
  "name": "turnkey-rail",
  "version": "0.1.0",
  "private": true,
  "description": "Turnkey Rail — construction management app",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=22"
  }
}
